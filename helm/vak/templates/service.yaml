apiVersion: v1
kind: Service
metadata:
  name: {{ include "vak.fullname" . }}
  labels:
    {{- include "vak.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vak.selectorLabels" . | nindent 4 }}
